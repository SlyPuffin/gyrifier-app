import React from 'react'

// this isn't strictly necessary, as the links from each deck can point to the 
// edit component and automatically pass it the information needed. 

function DeckEditor() {
  return (
    <div className="text-4xl px-8 py-2 text-orangeweboxfordblue-primary">
        decks/edit/index
    </div>
  )
}

export default DeckEditor